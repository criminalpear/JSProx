{
  "name": "UV Console Proxy",
  "image": "mcr.microsoft.com/devcontainers/javascript-node:20",

  // Install the Ultraviolet stack once when the Codespace is created.
  "postCreateCommand": "cd ultraviolet && npm install",

  // Start the proxy automatically each time you attach. Runs detached; logs to /tmp/uv.log.
  "postAttachCommand": "cd ultraviolet && (nohup npm start > /tmp/uv.log 2>&1 &) && echo 'UV Console Proxy starting on port 8080 — open the forwarded 8080 URL.'",

  "forwardPorts": [8080],
  "portsAttributes": {
    "8080": {
      "label": "UV Console Proxy",
      "onAutoForward": "notify"
    }
  },

  "customizations": {
    "vscode": {
      "extensions": ["dbaeumer.vscode-eslint"]
    }
  }
}
